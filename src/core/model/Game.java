/**
 * Game Enum
 * The program at many points checks what game is currently running in order to set things up. This makes that slightly easier.
 * 
 * @package core.model
 * @author Jordan Robinson, Ryan Carter
 * @version v1.0.1.0 15 Feb 2012
 */

package core.model;

public enum Game { 
	SENET,
	CHECKERS,
	MANCALA;
	//add in more games in the same format if required at a later date
}
